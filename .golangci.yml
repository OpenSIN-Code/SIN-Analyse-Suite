linters:
  enable:
    - gofmt
    - goimports
    - govet
    - errcheck
    - staticcheck
    - gosec
issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - gosec
run:
  timeout: 5m
